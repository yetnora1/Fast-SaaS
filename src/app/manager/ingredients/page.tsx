"use client";
import { UsageVarianceReport } from "@/components/UsageVarianceReport";

/** Manager view — same report; the API allows cafe_manager alongside the owner. */
export default function ManagerIngredientsPage() {
  return <UsageVarianceReport />;
}
