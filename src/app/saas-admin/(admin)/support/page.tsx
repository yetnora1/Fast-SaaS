"use client";
import { SupportCenter } from "@/components/SupportCenter";

/** Cross-tenant inbox: every cafe's requests, each tagged with its cafe name. */
export default function SaasSupportPage() {
  return <SupportCenter side="platform" />;
}
